#--------------------------------------------------
#  发布
#--------------------------------------------------
#

name: Release

on:
  push:
    tags:
      - "*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 当前用户
        run: whoami